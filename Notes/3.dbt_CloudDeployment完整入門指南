# dbt Cloud Deployment 完整入門指南

## 🎯 為什麼需要 Deployment？

### 情境想像
想像你是一位廚師，在自己的廚房（開發環境）研發新菜色：

```
👨‍🍳 開發廚房（Development）     🍽️ 正式餐廳（Production）
- 可以隨意實驗               - 顧客用餐的地方
- 失敗了重來就好             - 不能出錯
- 食材浪費沒關係             - 每道菜都要完美
```

**dbt Deployment 就是這個概念**：
- **開發環境**：你可以隨意測試、實驗，壞掉也沒關係
- **生產環境**：給真實使用者使用的正式資料，絕對不能出錯

## 🏗️ 軟體開發最佳實踐

### 傳統 Data Team 的問題
```
❌ 過去常見的做法：
- 直接在正式資料庫上改東改西
- 沒有版本控制概念
- 不知道誰改了什麼
- 一個人出錯，全部人受影響
- 沒有測試環境
```

### dbt 帶來的改變
```
✅ 現代軟體開發標準：
- 開發 → 測試 → 部署的標準流程
- 強制版本控制（Git）
- 環境隔離（Dev vs Prod）
- 自動化部署
- 可追蹤的變更歷史
```

## 🔄 開發到部署的完整流程

### 階段一：開發（Development）
```
你的電腦/dbt Cloud IDE
        ↓
在 dbt_dev dataset 測試
        ↓
Commit & Push 到 GitHub
```

### 階段二：部署（Deployment）  
```
GitHub main branch
        ↓
dbt Cloud 自動抓取最新 code
        ↓
在 dbt_prod dataset 執行
        ↓
生成正式的 tables/views
```

## 🛠️ 實際設定步驟

### 前置作業：確保程式碼已合併
```bash
# 確保所有變更都在 main branch
git status                    # 檢查是否有未提交的變更
git add .                     # 加入所有變更
git commit -m "準備部署"       # 提交變更
# 在 GitHub 上建立 Pull Request
# 合併到 main branch
```

### 步驟 1：建立 Deployment Environment

#### 1.1 進入環境設定
```
dbt Cloud → Deploy → Environments
```

目前狀況：
- ✅ **Development Environment** 已存在
  - Dataset: `dbt_dev`
  - 用途：開發和測試

#### 1.2 建立生產環境
```
點選「Create Environment」
```

**Environment 設定：**
- **Name**: Production
- **Environment Type**: Deployment  
- **Dataset**: `dbt_prod`（自動產生）
- **Git Branch**: main（正式環境一定用 main branch）

#### 1.3 環境差異對比
```
Development Environment:
├── Dataset: dbt_dev
├── Branch: demo（或其他開發分支）
├── 用途: 開發測試
└── 影響: 只影響開發者

Production Environment:  
├── Dataset: dbt_prod
├── Branch: main
├── 用途: 正式資料供應
└── 影響: 影響所有使用者
```

### 步驟 2：建立 Deployment Job

#### 2.1 進入 Job 設定
```
dbt Cloud → Deploy → Jobs
```

#### 2.2 建立新 Job
```
點選「Create Job」
```

**Job 設定：**
- **Job Name**: `Default Build`
- **Description**: `Build all dbt models`
- **Environment**: 選擇剛建立的 Production environment
- **Commands**: 保持預設（通常是 `dbt run`）

#### 2.3 Job 的核心概念
```
Job = 自動化的 dbt 指令執行器

Manual Job:
你手動點 "Run Now" → 立即執行

Scheduled Job:
設定時間（如每天早上 8 點） → 自動執行
```

### 步驟 3：執行第一次部署

#### 3.1 手動執行
```
點選「Run Now」
```

#### 3.2 監控執行過程
Job 執行時會有幾個階段：

```
1. 🔄 Clone Git Repository
   - 從 GitHub 下載最新的 main branch code
   - 確保使用最新版本

2. 🏗️ dbt run  
   - 執行所有 dbt models
   - 在 dbt_prod dataset 建立 tables/views

3. ✅ Success
   - 所有步驟完成
   - 顯示綠色勾勾
```

#### 3.3 查看執行結果
點進去 Job 詳情可以看到：
- **各步驟執行狀態**
- **使用的 Commit #**（版本控制）
- **執行時間**
- **詳細 logs**

## 🔍 驗證部署結果

### 在 BigQuery 檢查
執行完成後，你會看到：

#### 新的 Dataset
```
✅ dbt_prod (新建立的正式環境 dataset)
├── customers (table/view)
├── stg_customers (table/view)  
└── stg_orders (table/view)
```

#### 對比兩個環境
```
開發環境:                    正式環境:
dbt_dev                     dbt_prod
├── customers               ├── customers
├── stg_customers           ├── stg_customers
└── stg_orders             └── stg_orders

用途: 開發測試              用途: 正式資料供應
影響: 只有開發者            影響: 所有使用者
```

## 🔄 版本控制的強制性

### 每次執行都抓最新版本
```
Job 執行流程:
1. 連接到 GitHub
2. 下載 main branch 最新 commit
3. 使用該版本的 code 執行 dbt

結果: 強迫你一定要做版本控制
```

### 查看版本資訊
在 Job 列表可以看到：
- **執行時間**
- **使用的 Commit #**（如：`abc123f`）
- **執行結果**（成功/失敗）

### Git 工作流程變成必須
```
❌ 以前的做法:
隨意改 code → 直接跑 → 希望沒問題

✅ 現在的做法:
改 code → commit → push → merge to main → 自動部署
```

## 🏢 企業級的最佳實踐

### 環境隔離的好處

#### 1. 風險控制
```
開發環境壞掉 → 只影響開發者
正式環境穩定 → 使用者不受影響
```

#### 2. 資料一致性
```
正式環境的資料 = 可信賴的資料來源
所有報表和分析都基於一致的資料
```

#### 3. 變更可追蹤
```
每次部署都有 commit #
出問題時可以快速定位和回復
```

### 軟體工程師等級的資料工程

#### Before dbt:
```
👨‍💻 資料分析師:
- 直接在資料庫改 SQL
- 沒有版本控制
- 不知道環境管理
- 一人出錯，全隊受影響
```

#### After dbt:
```
🚀 資料工程師:
- 使用 Git 版本控制
- 環境隔離和管理
- 自動化部署流程
- 軟體工程最佳實踐
```

## ⚙️ 進階設定概念

### Scheduling（排程）
雖然今天是手動執行，但 Job 可以設定自動排程：

```
Daily at 8:00 AM:
- 自動執行 dbt run
- 更新所有正式環境的資料
- 確保報表使用最新資料
```

### Environment Variables
不同環境可以有不同的設定：

```yaml
# Development
target_schema: dbt_dev
database: dbt-tutorial-dev

# Production  
target_schema: dbt_prod
database: dbt-tutorial-prod
```

### Multiple Jobs
一個環境可以有多個 Job：

```
Build Job: dbt run（建立所有 models）
Test Job: dbt test（執行資料品質檢查）
Docs Job: dbt docs generate（產生文件）
```

## 🚨 常見問題與解決

### 問題 1：Job 執行失敗
```
原因: main branch 的 code 有錯誤
解決: 
1. 在開發環境先測試
2. 確認 dbt run 成功後再 merge
3. 檢查 commit history 找出問題版本
```

### 問題 2：找不到最新變更
```
原因: 忘記 commit 或 merge 到 main branch
解決:
1. 檢查 git status
2. 提交所有變更
3. 建立 Pull Request 合併到 main
```

### 問題 3：兩個環境結果不一致
```
原因: 開發環境和生產環境使用不同版本的 code
解決:
1. 確保開發環境的變更都已 commit
2. 在生產環境重新執行 Job
3. 比較兩邊的 Commit #
```

## 📊 監控和維護

### Job 執行歷史
```
每次執行的記錄包含:
- 執行時間
- 使用的 commit #  
- 成功/失敗狀態
- 詳細 logs
- 執行時長
```

### 失敗通知
可以設定：
- Email 通知
- Slack 通知  
- 其他 webhook

## 🎯 總結：現代資料工程的標準

### 核心價值觀
```
🔒 Reliability: 可靠性
- 正式環境絕對穩定
- 所有變更都經過測試

📝 Traceability: 可追蹤性  
- 每個變更都有版本記錄
- 問題發生時能快速定位

🔄 Reproducibility: 可重現性
- 同樣的 code 產生同樣的結果
- 環境一致性保證
```

### 思維轉變
```
從「個人工具」到「團隊系統」
從「隨意修改」到「標準流程」
從「資料分析師」到「資料工程師」
```

> **最重要的概念**：dbt 不只是工具，更是一套**思維方式**。它把軟體工程的最佳實踐帶入資料領域，讓資料團隊也能像軟體團隊一樣專業和可靠。
